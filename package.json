{
  "name": "pet-adoption-agency",
  "version": "1.0.0",
  "description": "pet adoption site",
  "main": "index.js",
  "scripts": {
    "start": "http-server --silent -o -a localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "praveen ningappa",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.2"
  }
}
